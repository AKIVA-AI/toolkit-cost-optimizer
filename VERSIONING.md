# Versioning policy (Toolkit Cost & Latency Optimizer)

Follows SemVer (see suite policy in `docs/enterprise-tools/VERSIONING.md`).

## Stability notes

- CLI output is designed to be human-readable; machine-readability should be handled via JSON output if/when added.
- Schema validation targets `docs/enterprise-tools/schemas/toolkit_inference_event.schema.json` (additive fields are allowed).


